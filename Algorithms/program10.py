import Util                                    # importing Util module

Amount = int(input('Enter the amount :'))       # Asks input  for amount
Util.Utilities.vendingMachine(Amount)          # calls staticmethod from the Utilities class for calculation 
